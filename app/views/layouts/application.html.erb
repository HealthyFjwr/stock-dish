<!DOCTYPE html>
<html>
  <head>
    <%= render "layouts/shared/head" %>
  </head>

  <body>
    <main class="<%= yield(:main_class) || "container mx-auto mt-28 px-5 flex" %>">
      <%= yield %>
    </main>
  </body>
</html>
